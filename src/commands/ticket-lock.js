const {SlashCommandBuilder,PermissionFlagsBits,mw,db}=require('./_helpers');module.exports={data:new SlashCommandBuilder().setName('ticket-lock').setDescription('Lock requester access'),execute:i=>mw.createMiddleware({permissions:[PermissionFlagsBits.ManageChannels]})(i,null,async()=>{const t=db.prepare('SELECT * FROM tickets WHERE channel_id=?').get(i.channel.id);if(!t)throw new Error('Not a ticket.');await i.channel.permissionOverwrites.edit(t.user_id,{SendMessages:false});await i.reply('🔒 Ticket locked for the requester.')})};
