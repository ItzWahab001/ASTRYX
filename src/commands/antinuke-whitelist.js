const {SlashCommandBuilder,PermissionFlagsBits,mw,db}=require('./_helpers');module.exports={data:new SlashCommandBuilder().setName('antinuke-whitelist').setDescription('Whitelist a user or role from anti-nuke').setDefaultMemberPermissions(PermissionFlagsBits.Administrator).addUserOption(o=>o.setName('user').setDescription('User')).addRoleOption(o=>o.setName('role').setDescription('Role')),execute:i=>mw.createMiddleware({permissions:[PermissionFlagsBits.Administrator]})(i,null,async()=>{const u=i.options.getUser('user'),r=i.options.getRole('role');if(!u&&!r)throw new Error('Provide a user or role.');db.prepare('INSERT OR IGNORE INTO security_whitelist(guild_id,user_id,role_id,kind) VALUES(?,?,?,?)').run(i.guild.id,u?.id||null,r?.id||null,u?'user':'role');await i.reply({content:'🛡️ Anti-nuke whitelist updated.',ephemeral:true})})};
