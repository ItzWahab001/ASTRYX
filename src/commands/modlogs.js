const {SlashCommandBuilder,PermissionFlagsBits,mw,db}=require('./_helpers');module.exports={data:new SlashCommandBuilder().setName('modlogs').setDescription('View recent moderation logs').addIntegerOption(o=>o.setName('limit').setDescription('1-20').setMinValue(1).setMaxValue(20)),execute:i=>mw.createMiddleware({permissions:[PermissionFlagsBits.ModerateMembers]})(i,null,async()=>{const n=i.options.getInteger('limit')||10;const rows=db.prepare('SELECT * FROM moderation_cases WHERE guild_id=? ORDER BY id DESC LIMIT ?').all(i.guild.id,n);await i.reply(rows.length?rows.map(x=>`#${x.id} • ${x.action} • <@${x.target_id}> • <@${x.moderator_id}> • ${x.reason||'No reason'}`).join('\n'):'No moderation logs.')})};
