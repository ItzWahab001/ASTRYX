const {SlashCommandBuilder,PermissionFlagsBits,mw}=require('./_helpers');const automod=require('../services/automod');module.exports={data:new SlashCommandBuilder().setName('automod-rules').setDescription('List AutoMod rules'),execute:i=>mw.createMiddleware({permissions:[PermissionFlagsBits.ManageGuild]})(i,null,async()=>{const rows=automod.list(i.guild.id);await i.reply(rows.length?rows.map(x=>`${x.name} • ${x.rule_id} • ${x.enabled?'enabled':'disabled'}`).join('\n'):'No DYNEX-managed AutoMod rules.')})};
