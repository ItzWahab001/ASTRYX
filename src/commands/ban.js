const {SlashCommandBuilder,PermissionFlagsBits,mw,mod}=require('./_helpers');module.exports={data:new SlashCommandBuilder().setName('ban').setDescription('Ban a member').addUserOption(o=>o.setName('user').setDescription('Target').setRequired(true)).addStringOption(o=>o.setName('reason').setDescription('Reason')).addStringOption(o=>o.setName('evidence').setDescription('Evidence/reference URL or note')),execute:i=>mw.createMiddleware({permissions:[PermissionFlagsBits.BanMembers],botPermissions:[PermissionFlagsBits.BanMembers]}) (i,null,async()=>{const m=await i.guild.members.fetch(i.options.getUser('user').id).catch(()=>null);const c=await mod.act(i,m,'ban',i.options.getString('reason')||'No reason provided',undefined,i.options.getString('evidence'));await i.reply(`🔨 Banned <@${m.id}> • Case #${c.id}`)})};
