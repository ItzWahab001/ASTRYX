const test=require('node:test');const assert=require('node:assert/strict');const TTLCache=require('../src/cache/ttl');test('TTL cache expires entries',async()=>{const c=new TTLCache();c.set('a',1,20);assert.equal(c.get('a'),1);await new Promise(r=>setTimeout(r,30));assert.equal(c.get('a'),undefined)});test('TTL cache evicts oldest when max exceeded',()=>{const c=new TTLCache(2);c.set('a',1,10000);c.set('b',2,10000);c.set('c',3,10000);assert.equal(c.get('a'),undefined);assert.equal(c.get('c'),3)});
