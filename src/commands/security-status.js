const {SlashCommandBuilder,PermissionFlagsBits,mw,db,EmbedBuilder,ActionRowBuilder,ButtonBuilder,ButtonStyle,guild,mod}=require('./_helpers');const music=require('../services/music');const ticket=require('../services/tickets');const repo=require('../repositories/guild');const levels=require('../services/levels');const eco=require('../services/economy');
const status={data:new SlashCommandBuilder().setName('security').setDescription('View DYNEX security status'),execute:i=>mw.createMiddleware({permissions:[PermissionFlagsBits.ManageGuild]})(i,null,async()=>{const s=require('../repositories/guild').get(i.guild.id);await i.reply(`🛡️ Anti-raid: **${s.antiraid_enabled?'ON':'OFF'}**\n💣 Anti-nuke: **${s.antinuke_enabled?'ON':'OFF'}**\n🧰 AutoMod: **${s.automod_enabled?'ON':'OFF'}**`)})};
module.exports=status;
