const {SlashCommandBuilder,PermissionFlagsBits,mw,db,EmbedBuilder,ActionRowBuilder,ButtonBuilder,ButtonStyle,guild,mod}=require('./_helpers');const music=require('../services/music');const ticket=require('../services/tickets');const repo=require('../repositories/guild');const levels=require('../services/levels');const eco=require('../services/economy');
const remove={data:new SlashCommandBuilder().setName('automod-delete').setDescription('Delete a DYNEX AutoMod rule').setDefaultMemberPermissions(PermissionFlagsBits.ManageGuild).addStringOption(o=>o.setName('id').setDescription('Rule ID').setRequired(true)),execute:i=>mw.createMiddleware({permissions:[PermissionFlagsBits.ManageGuild],botPermissions:[PermissionFlagsBits.ManageGuild]})(i,null,async()=>{await require('../services/automod').remove(i.guild,i.options.getString('id'));await i.reply('🗑️ AutoMod rule deleted.')})};
module.exports=remove;
