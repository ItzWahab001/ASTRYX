const {SlashCommandBuilder,PermissionFlagsBits,mw,db}=require('./_helpers');module.exports={data:new SlashCommandBuilder().setName('cases').setDescription('List recent moderation cases'),execute:i=>mw.createMiddleware({permissions:[PermissionFlagsBits.ModerateMembers]})(i,null,async()=>{const rows=db.prepare('SELECT id,target_id,action,reason FROM moderation_cases WHERE guild_id=? ORDER BY id DESC LIMIT 20').all(i.guild.id);await i.reply(rows.length?rows.map(x=>`#${x.id} • ${x.action} • <@${x.target_id}> • ${x.reason||'No reason'}`).join('\n'):'No cases.')})};
