const {SlashCommandBuilder,PermissionFlagsBits,mw,db}=require('./_helpers');module.exports={data:new SlashCommandBuilder().setName('ticket-stats').setDescription('View ticket statistics'),execute:i=>mw.createMiddleware({permissions:[PermissionFlagsBits.ManageChannels]})(i,null,async()=>{const x=db.prepare('SELECT COUNT(*) total,SUM(status="open") open,SUM(status="closed") closed,SUM(status="archived") archived FROM tickets WHERE guild_id=?').get(i.guild.id);await i.reply(`🎫 Total: **${x.total||0}** • Open: **${x.open||0}** • Closed: **${x.closed||0}** • Archived: **${x.archived||0}**`)})};
