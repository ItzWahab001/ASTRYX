const {SlashCommandBuilder,PermissionFlagsBits,mw,db,EmbedBuilder,ActionRowBuilder,ButtonBuilder,ButtonStyle,guild,mod}=require('./_helpers');const music=require('../services/music');const ticket=require('../services/tickets');const repo=require('../repositories/guild');const levels=require('../services/levels');const eco=require('../services/economy');
const work={data:new SlashCommandBuilder().setName('work').setDescription('Work for coins'),execute:i=>mw.createMiddleware({feature:'economy_enabled'})(i,{guild:require('../repositories/guild')},async()=>{const x=repo.row(i.guild.id,i.user.id);if(Date.now()-x.work_at<3600000)throw new Error('Work is still on cooldown.');const n=100+Math.floor(Math.random()*301);eco.reward(i.guild.id,i.user.id,'work',n);await i.reply(`💼 You earned **${n}** coins.`)})};
module.exports=work;
