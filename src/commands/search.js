const {SlashCommandBuilder}=require('discord.js');const {mw}=require('./_helpers');const music=require('../services/music');
module.exports={data:new SlashCommandBuilder().setName('search').setDescription('Search for a music track').addStringOption(o=>o.setName('query').setDescription('Search query').setRequired(true)),execute:i=>mw.createMiddleware({cooldownMs:1000})(i,null,async()=>{const t=await music.resolve(i.options.getString('query'));await i.reply(`🔎 **${t.title}**\n${t.author||'Unknown artist'} · ${t.duration?Math.floor(t.duration/60)+':'+String(Math.floor(t.duration%60)).padStart(2,'0'):'Unknown duration'}\n${t.url}`)})};
