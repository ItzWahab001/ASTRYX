const {SlashCommandBuilder,PermissionFlagsBits,mw,db,EmbedBuilder,ActionRowBuilder,ButtonBuilder,ButtonStyle,guild,mod}=require('./_helpers');const music=require('../services/music');const ticket=require('../services/tickets');const repo=require('../repositories/guild');const levels=require('../services/levels');const eco=require('../services/economy');
const antinuke={data:new SlashCommandBuilder().setName('antinuke').setDescription('Toggle anti-nuke').setDefaultMemberPermissions(PermissionFlagsBits.Administrator).addBooleanOption(o=>o.setName('enabled').setDescription('Enabled').setRequired(true)),execute:i=>mw.createMiddleware({permissions:[PermissionFlagsBits.Administrator]})(i,null,async()=>{require('../repositories/guild').update(i.guild.id,{antinuke_enabled:i.options.getBoolean('enabled')?1:0});await i.reply('💣 Anti-nuke configuration updated.')})};
module.exports=antinuke;
