const {SlashCommandBuilder,PermissionFlagsBits,mw,db,EmbedBuilder,ActionRowBuilder,ButtonBuilder,ButtonStyle,guild,mod}=require('./_helpers');const music=require('../services/music');const ticket=require('../services/tickets');const repo=require('../repositories/guild');const levels=require('../services/levels');const eco=require('../services/economy');
const leaderboard={data:new SlashCommandBuilder().setName('leaderboard').setDescription('XP leaderboard'),execute:i=>mw.createMiddleware({})(i,null,async()=>{const rows=db.prepare('SELECT user_id,level,xp FROM levels WHERE guild_id=? ORDER BY xp DESC LIMIT 10').all(i.guild.id);await i.reply(rows.length?rows.map((x,n)=>`**${n+1}.** <@${x.user_id}> • Lv.${x.level} • ${x.xp} XP`).join('\n'):'No XP yet.')})};
module.exports=leaderboard;
