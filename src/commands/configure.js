const {SlashCommandBuilder,PermissionFlagsBits,mw}=require('./_helpers');const repo=require('../repositories/guild');module.exports={data:new SlashCommandBuilder().setName('configure').setDescription('Set core DYNEX channels').setDefaultMemberPermissions(PermissionFlagsBits.ManageGuild).addChannelOption(o=>o.setName('modlog').setDescription('Moderation log')).addChannelOption(o=>o.setName('welcome').setDescription('Welcome channel')).addChannelOption(o=>o.setName('goodbye').setDescription('Goodbye channel')),execute:i=>mw.createMiddleware({permissions:[PermissionFlagsBits.ManageGuild]})(i,null,async()=>{const p={},a=i.options.getChannel('modlog'),b=i.options.getChannel('welcome'),c=i.options.getChannel('goodbye');if(a)p.mod_log_channel_id=a.id;if(b)p.welcome_channel_id=b.id;if(c)p.goodbye_channel_id=c.id;repo.update(i.guild.id,p);await i.reply({content:'⚙️ DYNEX channel configuration saved.',ephemeral:true})})};
