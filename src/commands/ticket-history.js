const {SlashCommandBuilder,PermissionFlagsBits,mw,db}=require('./_helpers');module.exports={data:new SlashCommandBuilder().setName('ticket-history').setDescription('View ticket history').addUserOption(o=>o.setName('user').setDescription('Requester')),execute:i=>mw.createMiddleware({permissions:[PermissionFlagsBits.ManageChannels]})(i,null,async()=>{const u=i.options.getUser('user');const rows=u?db.prepare('SELECT * FROM tickets WHERE guild_id=? AND user_id=? ORDER BY id DESC LIMIT 15').all(i.guild.id,u.id):db.prepare('SELECT * FROM tickets WHERE guild_id=? ORDER BY id DESC LIMIT 15').all(i.guild.id);await i.reply(rows.length?rows.map(x=>`#${x.id} • ${x.category} • ${x.status} • <#${x.channel_id}>`).join('\n'):'No ticket history.')})};
