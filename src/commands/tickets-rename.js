const {SlashCommandBuilder,PermissionFlagsBits,mw,db,EmbedBuilder,ActionRowBuilder,ButtonBuilder,ButtonStyle,guild,mod}=require('./_helpers');const music=require('../services/music');const ticket=require('../services/tickets');const repo=require('../repositories/guild');const levels=require('../services/levels');const eco=require('../services/economy');
const rename={data:new SlashCommandBuilder().setName('ticket-rename').setDescription('Rename this ticket').addStringOption(o=>o.setName('name').setDescription('New name').setRequired(true)),execute:i=>mw.createMiddleware({permissions:[PermissionFlagsBits.ManageChannels]})(i,null,async()=>{if(!i.channel.name.startsWith('ticket-'))throw new Error('Use this inside a ticket.');const name=i.options.getString('name').replace(/[^a-z0-9-]/gi,'-').slice(0,90);await i.channel.setName(name);await i.reply(`✏️ Renamed to **${name}**.`)})};
module.exports=rename;
