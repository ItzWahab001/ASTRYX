const {SlashCommandBuilder}=require('discord.js');const {mw}=require('./_helpers');const music=require('../services/music');
module.exports={data:new SlashCommandBuilder().setName('queue').setDescription('Show queue').addIntegerOption(o=>o.setName('page').setDescription('Page number').setMinValue(1)),execute:i=>mw.createMiddleware({})(i,null,async()=>{const p=music.manager.get(i.guild),page=p.queueManager.paginate(i.options.getInteger('page')||1,10);await i.reply(page.items.length?`🎶 Page ${page.page}/${page.pages}\n${page.items.map((x,n)=>`${(page.page-1)*10+n+1}. ${x.title}`).join('\n')}`:'Queue is empty.')})};
