const {SlashCommandBuilder,PermissionFlagsBits,mw,db,EmbedBuilder,ActionRowBuilder,ButtonBuilder,ButtonStyle,guild,mod}=require('./_helpers');const music=require('../services/music');const ticket=require('../services/tickets');const repo=require('../repositories/guild');const levels=require('../services/levels');const eco=require('../services/economy');
const daily={data:new SlashCommandBuilder().setName('daily').setDescription('Claim daily coins'),execute:i=>mw.createMiddleware({feature:'economy_enabled'})(i,{guild:require('../repositories/guild')},async()=>{const x=repo.row(i.guild.id,i.user.id);if(Date.now()-x.daily_at<86400000)throw new Error('Daily is still on cooldown.');eco.reward(i.guild.id,i.user.id,'daily',500);await i.reply('🎁 You received **500** coins.')})};
module.exports=daily;
