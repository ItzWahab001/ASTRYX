'use client'; import {useEffect,useState} from 'react'; import Link from 'next/link';
export default function Guilds(){const[g,setG]=useState<any[]>([]);const[e,setE]=useState('');useEffect(()=>{fetch('/api/guilds').then(async r=>{if(!r.ok)throw new Error('Please login again.');return r.json()}).then(setG).catch(x=>setE(x.message))},[]);return <main className="shell"><header className="top"><div className="brand">DYN<span>EX</span></div><a className="btn" href="/">Home</a></header><div className="content"><div className="hero"><div><div className="eyebrow">Server selection</div><h1 className="title">Choose a server</h1><p className="muted">Only servers where your Discord OAuth2 account has Manage Server or ownership are shown.</p></div></div>{e&&<div className="notice">{e}</div>}<div className="list">{g.map(x=><div className="card guild" key={x.id}><div><div className="guildname">{x.name}</div><span className="badge">{x.owner?'Owner':'Manage Server'}</span></div><Link className="btn primary" href={`/guilds/${x.id}`}>Open</Link></div>)}{!g.length&&!e&&<div className="card"><h3>No manageable servers</h3><p className="muted">Authorize DYNEX with a Discord account that can manage a server.</p></div>}</div></div></main>}
