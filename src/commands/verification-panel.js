const {SlashCommandBuilder,PermissionFlagsBits,mw,db,EmbedBuilder,ActionRowBuilder,ButtonBuilder,ButtonStyle,guild,mod}=require('./_helpers');const music=require('../services/music');const ticket=require('../services/tickets');const repo=require('../repositories/guild');const levels=require('../services/levels');const eco=require('../services/economy');
const panel={data:new SlashCommandBuilder().setName('verification-panel').setDescription('Post verification panel').setDefaultMemberPermissions(PermissionFlagsBits.ManageGuild),execute:i=>mw.createMiddleware({permissions:[PermissionFlagsBits.ManageGuild]})(i,null,async()=>{const s=repo.get(i.guild.id);if(!s.verification_role_id)throw new Error('Run /verification-setup first.');await i.reply({embeds:[new EmbedBuilder().setColor(0x57f287).setTitle('DYNEX Verification').setDescription('Click verify to receive the configured verified role.')],components:[new ActionRowBuilder().addComponents(new ButtonBuilder().setCustomId('verify:me').setLabel('Verify').setStyle(ButtonStyle.Success))]})})};
module.exports=panel;
