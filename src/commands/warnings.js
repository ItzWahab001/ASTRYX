const {SlashCommandBuilder,PermissionFlagsBits,mw,mod,EmbedBuilder}=require('./_helpers');module.exports={data:new SlashCommandBuilder().setName('warnings').setDescription('View warnings').addUserOption(o=>o.setName('user').setDescription('Target').setRequired(true)),execute:i=>mw.createMiddleware({permissions:[PermissionFlagsBits.ModerateMembers]})(i,null,async()=>{const u=i.options.getUser('user'),rows=mod.warnings(i.guild.id,u.id);await i.reply({embeds:[new EmbedBuilder().setColor(0xfee75c).setTitle(`Warnings • ${u.tag}`).setDescription(rows.length?rows.map((r,n)=>`**${n+1}.** ${r.reason} • <@${r.moderator_id}>`).join('\n'):'No warnings.')]} )})};
