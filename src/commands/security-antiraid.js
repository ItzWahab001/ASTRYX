const {SlashCommandBuilder,PermissionFlagsBits,mw,db,EmbedBuilder,ActionRowBuilder,ButtonBuilder,ButtonStyle,guild,mod}=require('./_helpers');const music=require('../services/music');const ticket=require('../services/tickets');const repo=require('../repositories/guild');const levels=require('../services/levels');const eco=require('../services/economy');
const antiraid={data:new SlashCommandBuilder().setName('antiraid').setDescription('Toggle anti-raid').setDefaultMemberPermissions(PermissionFlagsBits.ManageGuild).addBooleanOption(o=>o.setName('enabled').setDescription('Enabled').setRequired(true)),execute:i=>mw.createMiddleware({permissions:[PermissionFlagsBits.ManageGuild]})(i,null,async()=>{require('../repositories/guild').update(i.guild.id,{antiraid_enabled:i.options.getBoolean('enabled')?1:0});await i.reply('🛡️ Anti-raid configuration updated.')})};
module.exports=antiraid;
