const {SlashCommandBuilder,PermissionFlagsBits,mw,EmbedBuilder}=require('./_helpers');const {db}=require('../database/db');const crypto=require('crypto');const cmd={data:new SlashCommandBuilder().setName('giveaway').setDescription('Create a restart-safe giveaway').setDefaultMemberPermissions(PermissionFlagsBits.ManageGuild).addStringOption(o=>o.setName('prize').setDescription('Prize').setRequired(true)).addIntegerOption(o=>o.setName('minutes').setDescription('Duration').setMinValue(1).setMaxValue(10080).setRequired(true)).addIntegerOption(o=>o.setName('winners').setDescription('Winner count').setMinValue(1).setMaxValue(20).setRequired(true)).addRoleOption(o=>o.setName('required_role').setDescription('Required role')),execute:i=>mw.createMiddleware({permissions:[PermissionFlagsBits.ManageGuild]})(i,null,async()=>{const prize=i.options.getString('prize'),minutes=i.options.getInteger('minutes'),winners=i.options.getInteger('winners'),role=i.options.getRole('required_role'),ends=Date.now()+minutes*60000;const msg=await i.reply({embeds:[new EmbedBuilder().setColor(0xeb459e).setTitle('🎉 Giveaway').setDescription(`**${prize}**\nEnds <t:${Math.floor(ends/1000)}:R>\nWinners: **${winners}**\nReact with 🎉 to enter.`)],fetchReply:true});await msg.react('🎉');db.prepare('INSERT INTO giveaways(guild_id,channel_id,message_id,prize,winner_count,ends_at,required_role_id) VALUES(?,?,?,?,?,?,?)').run(i.guild.id,i.channel.id,msg.id,prize,winners,ends,role?.id||null)})};module.exports=cmd;
