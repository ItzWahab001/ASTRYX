const {SlashCommandBuilder,PermissionFlagsBits,mw,mod}=require('./_helpers');module.exports={data:new SlashCommandBuilder().setName('kick').setDescription('Kick a member').addUserOption(o=>o.setName('user').setDescription('Target').setRequired(true)).addStringOption(o=>o.setName('reason').setDescription('Reason')).addStringOption(o=>o.setName('evidence').setDescription('Evidence/reference URL or note')),execute:i=>mw.createMiddleware({permissions:[PermissionFlagsBits.KickMembers],botPermissions:[PermissionFlagsBits.KickMembers]})(i,null,async()=>{const m=await i.guild.members.fetch(i.options.getUser('user').id).catch(()=>null);const c=await mod.act(i,m,'kick',i.options.getString('reason')||'No reason provided',undefined,i.options.getString('evidence'));await i.reply(`👢 Kicked <@${m.id}> • Case #${c.id}`)})};
