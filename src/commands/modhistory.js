const {SlashCommandBuilder,PermissionFlagsBits,mw,db,EmbedBuilder}=require('./_helpers');module.exports={data:new SlashCommandBuilder().setName('modhistory').setDescription('View moderation history').addUserOption(o=>o.setName('user').setDescription('Target').setRequired(true)),execute:i=>mw.createMiddleware({permissions:[PermissionFlagsBits.ModerateMembers]})(i,null,async()=>{const u=i.options.getUser('user');const rows=db.prepare('SELECT * FROM moderation_cases WHERE guild_id=? AND target_id=? ORDER BY id DESC LIMIT 15').all(i.guild.id,u.id);await i.reply({embeds:[new EmbedBuilder().setTitle(`Mod History • ${u.tag}`).setDescription(rows.length?rows.map(x=>`#${x.id} • **${x.action}** • ${x.reason||'No reason'} • <@${x.moderator_id}>`).join('\n'):'No cases.').setColor(0x5865f2)]})})};
