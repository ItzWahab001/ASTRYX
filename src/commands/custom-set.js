const {SlashCommandBuilder,PermissionFlagsBits,mw}=require('./_helpers');const repo=require('../repositories/custom');module.exports={data:new SlashCommandBuilder().setName('custom-set').setDescription('Create/update a custom prefix command').setDefaultMemberPermissions(PermissionFlagsBits.ManageGuild).addStringOption(o=>o.setName('name').setDescription('Name').setRequired(true)).addStringOption(o=>o.setName('response').setDescription('Response').setRequired(true)),execute:i=>mw.createMiddleware({permissions:[PermissionFlagsBits.ManageGuild]})(i,null,async()=>{const n=i.options.getString('name').toLowerCase();if(!/^[a-z0-9_-]{1,32}$/.test(n))throw new Error('Invalid command name.');repo.upsert(i.guild.id,n,i.options.getString('response'));await i.reply({content:'Custom command '+n+' saved.',ephemeral:true})})};
