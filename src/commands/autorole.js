const {SlashCommandBuilder,PermissionFlagsBits,mw,db}=require('./_helpers');module.exports={data:new SlashCommandBuilder().setName('autorole').setDescription('Configure automatic join role').setDefaultMemberPermissions(PermissionFlagsBits.ManageRoles).addRoleOption(o=>o.setName('role').setDescription('Role').setRequired(true)).addBooleanOption(o=>o.setName('enabled').setDescription('Enable or disable').setRequired(true)),execute:i=>mw.createMiddleware({permissions:[PermissionFlagsBits.ManageRoles],botPermissions:[PermissionFlagsBits.ManageRoles]})(i,null,async()=>{const role=i.options.getRole('role'),enabled=i.options.getBoolean('enabled');if(role.position>=i.guild.members.me.roles.highest.position)throw new Error('That role is above my highest role.');if(enabled)db.prepare('INSERT OR IGNORE INTO autoroles(guild_id,role_id) VALUES(?,?)').run(i.guild.id,role.id);else db.prepare('DELETE FROM autoroles WHERE guild_id=? AND role_id=?').run(i.guild.id,role.id);await i.reply({content:`Autorole ${enabled?'enabled':'disabled'} for ${role}.`,ephemeral:true})})};