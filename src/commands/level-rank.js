const {SlashCommandBuilder,PermissionFlagsBits,mw,db,EmbedBuilder,ActionRowBuilder,ButtonBuilder,ButtonStyle,guild,mod}=require('./_helpers');const music=require('../services/music');const ticket=require('../services/tickets');const repo=require('../repositories/guild');const levels=require('../services/levels');const eco=require('../services/economy');
const rank={data:new SlashCommandBuilder().setName('rank').setDescription('View rank').addUserOption(o=>o.setName('user').setDescription('User')),execute:i=>mw.createMiddleware({})(i,null,async()=>{const u=i.options.getUser('user')||i.user,x=db.prepare('SELECT * FROM levels WHERE guild_id=? AND user_id=?').get(i.guild.id,u.id)||{xp:0,level:0};await i.reply({embeds:[new EmbedBuilder().setColor(0x5865f2).setTitle(`${u.username} • Rank`).addFields({name:'Level',value:String(x.level),inline:true},{name:'XP',value:String(x.xp),inline:true})]})})};
module.exports=rank;
