const {SlashCommandBuilder,PermissionFlagsBits,mw,db,EmbedBuilder,ActionRowBuilder,ButtonBuilder,ButtonStyle,guild,mod}=require('./_helpers');const music=require('../services/music');const ticket=require('../services/tickets');const repo=require('../repositories/guild');const levels=require('../services/levels');const eco=require('../services/economy');
const logs={data:new SlashCommandBuilder().setName('security-logs').setDescription('View recent security incidents'),execute:i=>mw.createMiddleware({permissions:[PermissionFlagsBits.ManageGuild]})(i,null,async()=>{const rows=db.prepare('SELECT * FROM security_events WHERE guild_id=? ORDER BY id DESC LIMIT 20').all(i.guild.id);await i.reply(rows.length?rows.map(x=>`#${x.id} • ${x.event_type} • <@${x.actor_id}> • ${x.details||''}`).join('\n'):'No security incidents.')})};
module.exports=logs;
