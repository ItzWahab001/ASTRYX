const {SlashCommandBuilder,PermissionFlagsBits,mw,db,EmbedBuilder,ActionRowBuilder,ButtonBuilder,ButtonStyle,guild,mod}=require('./_helpers');const music=require('../services/music');const ticket=require('../services/tickets');const repo=require('../repositories/guild');const levels=require('../services/levels');const eco=require('../services/economy');
const setup={data:new SlashCommandBuilder().setName('verification-setup').setDescription('Configure verification').setDefaultMemberPermissions(PermissionFlagsBits.ManageGuild).addChannelOption(o=>o.setName('channel').setDescription('Verification channel').setRequired(true)).addRoleOption(o=>o.setName('verified_role').setDescription('Verified role').setRequired(true)).addRoleOption(o=>o.setName('unverified_role').setDescription('Unverified role')),execute:i=>mw.createMiddleware({permissions:[PermissionFlagsBits.ManageGuild]})(i,null,async()=>{repo.update(i.guild.id,{verification_channel_id:i.options.getChannel('channel').id,verification_role_id:i.options.getRole('verified_role').id,unverified_role_id:i.options.getRole('unverified_role')?.id||null});await i.reply({content:'Verification settings saved.',ephemeral:true})})};
module.exports=setup;
