const {EmbedBuilder}=require('discord.js');const repo=require('../repositories/guild');const security=require('../services/security');const logger=require('../core/logger');
module.exports={name:'guildMemberRemove',async execute(member){security.leave(member.guild);await security.recoverExpiredRaid(member.guild);const s=repo.ensure(member.guild.id);if(!s.goodbye_channel_id)return;const ch=member.guild.channels.cache.get(s.goodbye_channel_id);if(ch?.isTextBased())try{await ch.send({embeds:[new EmbedBuilder().setColor(0xed4245).setTitle('Goodbye').setDescription(s.goodbye_message.replaceAll('{user}',member.user.tag).replaceAll('{server}',member.guild.name))]})}catch(error){logger.warn({err:error,channel:ch.id},'goodbye message failed')}}};
