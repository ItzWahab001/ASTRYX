const {SlashCommandBuilder,PermissionFlagsBits,mw,db,EmbedBuilder,ActionRowBuilder,ButtonBuilder,ButtonStyle,guild,mod}=require('./_helpers');const music=require('../services/music');const ticket=require('../services/tickets');const repo=require('../repositories/guild');const levels=require('../services/levels');const eco=require('../services/economy');
const transactions={data:new SlashCommandBuilder().setName('transactions').setDescription('View your economy transactions'),execute:i=>mw.createMiddleware({})(i,null,async()=>{const rows=repo.transaction?require('../database/db').db.prepare('SELECT * FROM economy_transactions WHERE guild_id=? AND (from_user_id=? OR to_user_id=?) ORDER BY id DESC LIMIT 15').all(i.guild.id,i.user.id,i.user.id):[];await i.reply(rows.length?rows.map(x=>`#${x.id} • ${x.type} • ${x.amount} coins`).join('\n'):'No transactions.')})};
module.exports=transactions;
