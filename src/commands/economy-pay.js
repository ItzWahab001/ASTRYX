const {SlashCommandBuilder,PermissionFlagsBits,mw,db,EmbedBuilder,ActionRowBuilder,ButtonBuilder,ButtonStyle,guild,mod}=require('./_helpers');const music=require('../services/music');const ticket=require('../services/tickets');const repo=require('../repositories/guild');const levels=require('../services/levels');const eco=require('../services/economy');
const pay={data:new SlashCommandBuilder().setName('pay').setDescription('Transfer coins').addUserOption(o=>o.setName('user').setDescription('Recipient').setRequired(true)).addIntegerOption(o=>o.setName('amount').setDescription('Amount').setMinValue(1).setRequired(true)),execute:i=>mw.createMiddleware({feature:'economy_enabled'})(i,{guild:require('../repositories/guild')},async()=>{const u=i.options.getUser('user'),n=i.options.getInteger('amount');if(u.bot||u.id===i.user.id)throw new Error('Choose another user.');eco.pay(i.guild.id,i.user.id,u.id,n);await i.reply(`💸 Transferred **${n}** coins to ${u}.`)})};
module.exports=pay;
