const {SlashCommandBuilder,PermissionFlagsBits,mw,db}=require('./_helpers');module.exports={data:new SlashCommandBuilder().setName('ticket-unlock').setDescription('Unlock requester access'),execute:i=>mw.createMiddleware({permissions:[PermissionFlagsBits.ManageChannels]})(i,null,async()=>{const t=db.prepare('SELECT * FROM tickets WHERE channel_id=?').get(i.channel.id);if(!t)throw new Error('Not a ticket.');await i.channel.permissionOverwrites.edit(t.user_id,{ViewChannel:true,SendMessages:true,ReadMessageHistory:true});await i.reply('🔓 Ticket unlocked for the requester.')})};
