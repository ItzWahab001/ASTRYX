const {SlashCommandBuilder,PermissionFlagsBits,mw}=require('./_helpers');const repo=require('../repositories/guild');module.exports={data:new SlashCommandBuilder().setName('ticket-setup').setDescription('Configure tickets').setDefaultMemberPermissions(PermissionFlagsBits.ManageChannels).addChannelOption(o=>o.setName('category').setDescription('Category').setRequired(true)).addRoleOption(o=>o.setName('staff').setDescription('Staff role').setRequired(true)).addChannelOption(o=>o.setName('transcript').setDescription('Transcript channel')).addChannelOption(o=>o.setName('archive').setDescription('Archive category')),execute:i=>mw.createMiddleware({permissions:[PermissionFlagsBits.ManageChannels]})(i,null,async()=>{repo.update(i.guild.id,{ticket_category_id:i.options.getChannel('category').id,ticket_staff_role_id:i.options.getRole('staff').id,ticket_transcript_channel_id:i.options.getChannel('transcript')?.id||null,ticket_archive_category_id:i.options.getChannel('archive')?.id||null});await i.reply({content:'🎫 Ticket configuration saved.',ephemeral:true})})};
