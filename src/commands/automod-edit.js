const {SlashCommandBuilder,PermissionFlagsBits}=require('discord.js');const {mw}=require('./_helpers');const automod=require('../services/automod');module.exports={data:new SlashCommandBuilder().setName('automod-edit').setDescription('Edit a native AutoMod rule').setDefaultMemberPermissions(PermissionFlagsBits.ManageGuild).addStringOption(o=>o.setName('id').setDescription('Rule ID').setRequired(true)).addStringOption(o=>o.setName('type').setDescription('Rule type').setRequired(true).addChoices({name:'Keyword',value:'keyword'},{name:'Regex',value:'regex'},{name:'Spam',value:'spam'},{name:'Mention spam',value:'mention_spam'})).addStringOption(o=>o.setName('value').setDescription('Keywords/patterns or mention limit').setRequired(false)),execute:i=>mw.createMiddleware({permissions:[PermissionFlagsBits.ManageGuild],botPermissions:[PermissionFlagsBits.ManageGuild]})(i,null,async()=>{const type=i.options.getString('type'),v=i.options.getString('value')||'',input={name:`DYNEX ${type}`,type};if(type==='keyword')input.keywords=v.split(',').map(x=>x.trim()).filter(Boolean);if(type==='regex')input.patterns=v.split(',').map(x=>x.trim()).filter(Boolean);if(type==='mention_spam')input.mentionLimit=Number(v)||5;const r=await automod.edit(i.guild,i.options.getString('id'),input);await i.reply(`✏️ AutoMod rule updated: **${r.name}**`)})};
