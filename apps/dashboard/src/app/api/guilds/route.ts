import {NextRequest,NextResponse} from 'next/server';
export async function GET(req:NextRequest){const token=req.cookies.get('dynex_oauth')?.value;if(!token)return NextResponse.json({error:'unauthorized'},{status:401});const r=await fetch('https://discord.com/api/v10/users/@me/guilds',{headers:{authorization:`Bearer ${token}`},cache:'no-store'});if(!r.ok)return NextResponse.json({error:'discord_request_failed'},{status:r.status});const guilds=await r.json() as any[];const manageable=guilds.filter(g=>(BigInt(g.permissions??'0') & BigInt(0x20))===BigInt(0x20) || g.owner);return NextResponse.json(manageable.map(g=>({id:g.id,name:g.name,icon:g.icon,owner:g.owner,permissions:g.permissions})))}
