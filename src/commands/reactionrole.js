const {SlashCommandBuilder,PermissionFlagsBits,mw,ActionRowBuilder,ButtonBuilder,ButtonStyle}=require('./_helpers');const {db}=require('../database/db');module.exports={data:new SlashCommandBuilder().setName('reactionrole').setDescription('Create a role button').setDefaultMemberPermissions(PermissionFlagsBits.ManageRoles).addRoleOption(o=>o.setName('role').setDescription('Role').setRequired(true)).addStringOption(o=>o.setName('label').setDescription('Button label').setRequired(true)),execute:i=>mw.createMiddleware({permissions:[PermissionFlagsBits.ManageRoles],botPermissions:[PermissionFlagsBits.ManageRoles]})(i,null,async()=>{const role=i.options.getRole('role');if(role.position>=i.guild.members.me.roles.highest.position)throw new Error('That role is above my highest role.');const label=i.options.getString('label');const msg=await i.reply({content:`Click to receive/remove ${role}.`,components:[new ActionRowBuilder().addComponents(new ButtonBuilder().setCustomId(`rr:${role.id}`).setLabel(label).setStyle(ButtonStyle.Primary))],fetchReply:true});db.prepare('INSERT OR REPLACE INTO reaction_roles(guild_id,message_id,role_id,label) VALUES(?,?,?,?)').run(i.guild.id,msg.id,role.id,label)})};
