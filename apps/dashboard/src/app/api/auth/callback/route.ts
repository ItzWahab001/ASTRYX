import {NextRequest,NextResponse} from 'next/server'; import {env} from '@dynex/config';
export async function GET(req:NextRequest){const code=req.nextUrl.searchParams.get('code');if(!code)return NextResponse.redirect(new URL('/login?error=missing_code',req.url));const body=new URLSearchParams({client_id:env.DISCORD_CLIENT_ID,client_secret:env.DISCORD_CLIENT_SECRET,grant_type:'authorization_code',code,redirect_uri:env.DISCORD_REDIRECT_URI});const token=await fetch('https://discord.com/api/oauth2/token',{method:'POST',headers:{'content-type':'application/x-www-form-urlencoded'},body}).then(r=>r.json()) as any;if(!token.access_token)return NextResponse.redirect(new URL('/login?error=oauth_failed',req.url));const r=NextResponse.redirect(new URL('/guilds',req.url));r.cookies.set('dynex_oauth',token.access_token,{httpOnly:true,secure:process.env.NODE_ENV==='production',sameSite:'lax',maxAge:Math.min(token.expires_in??604800,604800),path:'/'});return r}
